You are triaging and filing tonight's batch of new personal emails into this Obsidian vault.

{{DATE_CONTEXT}}

1. Read the staged batch at /work/staging/personal/personal.md. It contains one
   section per new email, each headed with From / Subject / Received / Message-ID,
   followed by that email's body (long bodies are truncated with a notice).
2. Follow the vault conventions from Filing_Rules.md, provided in your system prompt if
   the file exists - it documents the concrete folder taxonomy, daily-note format, tag
   vocabulary, linking style, and create-vs-update rule.
3. Read /work/staging/personal/vault_index.txt - a complete, current list of every note
   path in the vault. Use it as your primary create-vs-update tool: shortlist existing
   candidate notes by title and folder from the index, then Read only those specific
   candidates to confirm. Do NOT run broad vault-wide Grep searches - the index already
   gives you complete title coverage.

TRIAGE (decide per email, before filing anything):
   - SKIP (do not file) noise: marketing and promotions, newsletters and digests,
     one-time passcodes / verification codes, automated system notifications and
     alerts, social-media notifications, and routine receipts/order confirmations
     with no lasting significance.
   - FILE anything with lasting personal significance: genuine personal
     correspondence, plans and bookings (travel, appointments, reservations),
     finance / health / legal events, and any commitment, decision, deadline, or
     action item - yours or someone else's - that you would want a record of.
   - When genuinely unsure, FILE it: a false skip loses information permanently,
     a false file is cheap to ignore. But record every skip with its reason.

FILING (for each email you decided to FILE):
4. Determine that email's DATE from its Received header, and file its entry under
   that date's daily note - creating the daily note if absent. Different emails in the
   batch may belong to different daily notes. Use the YYYY-MM-DD (MMM) date format for
   any date you write, and compute weekdays from the date itself, anchored to the DATE
   CONTEXT above - never guess a weekday.
5. IDEMPOTENCY: before filing an email, check whether a marker
   <!-- personal-email:<Message-ID> --> already exists in that date's daily note. If it
   does, treat the email as already filed and skip it (count it under skipped with
   reason "already filed"). Otherwise, append the marker on its own line together with
   the entry you file, so a re-run never double-files the same email.
   - **DATA PRESERVATION RULE (LOSSLESS)**: For emails you file, do not summarize, compress, or omit substantive details. Preserve specific identifiers (booking references, amounts, dates, ticket/order numbers), names of people, exact quotes, and concrete action items/options. Only omit true noise (email signatures, tracking footers, unsubscribe/confidentiality boilerplate).
   - **NOTE GRANULARITY RULE**: When a filed email belongs in a topic/project note, use highly specific note titles that describe the exact matter (e.g. 'Flight booking SQ317 to London 2026-08', NOT a generic 'Travel'). You MUST NOT append a distinct matter into a generic umbrella note just because they share a theme. Create a new specific note unless the email is a direct continuation of that *exact same* matter. Actively discover related existing notes via the index and embed `[[Note Title]]` links to inter-connect people, bookings, and threads.
   - **FILENAME RULE**: Note titles become filenames and wikilink targets. Never use the characters # ^ | [ ] : \ / in a note title (e.g. 'Purview DLP escalation - TrackingID 2606080040000512', NOT '... - TrackingID#2606080040000512'): '#', '^', '|' and brackets are Obsidian link syntax, so links to such notes silently break; ': \ /' are invalid in filenames on some platforms.
   - **LINK INTEGRITY RULE**: If you write a `[[New Note Title]]` link anywhere, you MUST also output/create that new note file. Never leave a dangling link. When creating the new note, you MUST populate it with relevant operational details and context; do not just leave it blank.
6. Append only. Never rewrite, reorder, or delete existing content in any note.
7. Work efficiently: don't re-read files you've already read, batch independent edits,
   and keep commentary between tool calls to a minimum.
8. Use your available tools (read, glob, grep, edit, write) to perform this task.
9. When finished, call the finish tool with:
   {"status": "filed" | "no_content", "work_date": "YYYY-MM-DD", "files_touched": [...],
    "entries_filed": N, "skipped": M, "skipped_details": ["<Subject> — <reason>", ...]}
   - entries_filed = emails actually filed; skipped = emails triaged out or already
     filed; skipped_details = one short line each (subject + reason). Set status to
     "no_content" only if the batch was entirely skipped (nothing filed).
