You are proposing how tonight's M365 Copilot work-summary digest should be filed into
this Obsidian vault. This is a DRY RUN: you must not modify any file under the vault.

{{DATE_CONTEXT}}

1. Read the staged digest at /work/staging/digest.md.
2. Follow the vault conventions from Filing_Rules.md, provided in your system prompt if
   the file exists - it documents the concrete folder taxonomy, daily-note format, tag
   vocabulary, linking style, and create-vs-update rule.
3. Read /work/staging/vault_index.txt - a complete, current list of every note in the
   vault, one per line as "YYYY-MM-DD<TAB>path" (the note's last-modified date). Use it
   as your primary create-vs-update tool: shortlist existing candidate notes by title and
   folder, then Read only those to confirm. A recently-modified note whose title matches a
   workstream in tonight's digest is very likely its continuation target. Never copy the
   date column into a filename or wikilink.
   - Also read /work/staging/recent_filing_log.md if it exists: it shows which notes recent
     runs filed into, so a recurring workstream keeps landing in the same canonical note.
   - Use search_relevant (thematic search) to locate the canonical note for a workstream
     when its title doesn't match the digest wording - at most one call per workstream. Use
     grep_search only for exact identifiers (ticket numbers). Do NOT run broad vault-wide
     greps otherwise - the index already gives complete title coverage.
4. Determine the WORK DATE. If the DATE CONTEXT above specifies one, use it exactly.
   Otherwise infer it from the digest content itself (each section is headed
   "Digest received <timestamp>" and the body usually states its own date, in a
   "Work Log - <DD Mon YYYY>" style line). Propose filing under that date's daily
   note and use that date in the idempotency marker - not today's date if the work
   date differs from it.
5. Check whether <!-- copilot-digest:<work-date> --> already exists in that daily note,
   and note in your proposal whether this run would be a no-op duplicate.
6. Split the digest into atomic entries and, for each, decide exactly where it would be
   filed: daily-note section, and which topic/project note (existing or new) it would
   update or create. Actively discover related existing notes via the index and embed `[[Note Title]]` links to inter-connect related concepts, issues, and tickets.
   - **DATA PRESERVATION RULE (LOSSLESS)**: Do not summarize, compress, or omit operational details. Preserve all specific identifiers (ticket numbers like CS... or TrackingID...), names of individuals, exact quotes, and specific action items/options from the source digest. Only omit true noise (email signatures, contact details, confidentiality disclaimers). Do not alter acronyms or department names (e.g. preserve "ITD / IFNRA" exactly as written).
   - **CHRONOLOGICAL RULE**: When writing any date with a weekday, compute the weekday from the work date itself, anchored to the DATE CONTEXT above (you know today's date and weekday from it - count backward/forward). Never guess a weekday; a wrong weekday next to a correct date is worse than no weekday.
   - **WORKSTREAM CONTINUITY RULE**: A recurring workstream - the same initiative, rollout, escalation, or ticket appearing across multiple days (e.g. "Zscaler global classified-file upload block", "Purview agency-rule creation") - should APPEND to its single canonical topic note, not a new note each day. Before proposing CREATE, check all three: (1) recent_filing_log.md, (2) a search_relevant call on the theme, (3) the index's recently-modified notes. Propose CREATE only when none surfaces a match.
   - **NOTE GRANULARITY RULE**: When creating new notes, use highly specific titles that describe the exact issue, task, or incident (e.g. 'RNS policy tips persist after licence removal', NOT generic titles like 'Purview policy tips limitation'). When deciding whether to UPDATE an existing note, you MUST NOT update notes with generic titles (like 'Purview policy tips limitation') just because they share a topic. If the digest introduces a specific scenario, test, or ticket, you MUST CREATE a new specific note rather than appending it to a generic umbrella note. Only update an existing note if it is a direct continuation of that *exact same specific issue or ticket*. (Tiebreaker: same initiative/ticket -> UPDATE the canonical note; a new specific scenario under a broad theme -> CREATE.)
   - **NEW-NOTE TEMPLATE**: For a proposed new note under Work/*/Troubleshooting/ or Work/*/Testing/, follow the vault template: H1 title, `**Date: <Weekday> <Do> <Month> <Year>**`, `##### keywords: ...`, `---`, then `### Who reported it:` / `### Problem statement:` / `### Troubleshooting steps taken:` / `### Solution:`, ending with `### Related` wikilinks. Never propose YAML frontmatter - this vault uses none.
   - **FILENAME RULE**: Note titles become filenames and wikilink targets. Never use the characters # ^ | [ ] : \ / in a note title (e.g. 'Purview DLP escalation - TrackingID 2606080040000512', NOT '... - TrackingID#2606080040000512'): '#', '^', '|' and brackets are Obsidian link syntax, so links to such notes silently break; ': \ /' are invalid in filenames on some platforms.
   - **LINK INTEGRITY RULE**: If you write a `[[New Note Title]]` link anywhere, you MUST also output/create that new note file. Never leave a dangling link. When creating the new note, you MUST populate it with relevant operational details and context; do not just leave it blank.
7. Do NOT use Write or Edit on any file inside the vault. Write your proposal to
   /work/staging/proposed.md - the only file you may write.
8. Begin proposed.md with a PLAN TABLE: one row per digest workstream/section listing the
   target note path and CREATE or UPDATE. Then, for each entry: one or two lines naming the
   target note, CREATE or UPDATE, and the content to append (the daily-note block and any
   new-note bodies may be shown in full, since they are the actual proposed text). Do not
   restate the digest separately, and do not explain reasoning unless a decision is
   genuinely ambiguous - flag only real ambiguities.
9. Work efficiently: don't re-read files you've already read, and keep commentary
   between tool calls to a minimum.
10. Use your available tools (read, glob, grep, write) to perform this task. Do not edit vault files.
11. When finished, call the finish tool with:
    {"status": "proposed" | "skipped_duplicate" | "no_content", "work_date": "YYYY-MM-DD", "files_touched": ["/work/staging/proposed.md"], "entries_filed": N}

