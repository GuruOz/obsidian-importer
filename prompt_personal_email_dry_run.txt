You are proposing how tonight's batch of new personal emails should be triaged and filed
into this Obsidian vault. This is a DRY RUN: you must not modify any file under the vault.

{{DATE_CONTEXT}}

1. Read the staged batch at /work/staging/personal/personal.md. It contains one
   section per new email, each headed with From / Subject / Received / Message-ID,
   followed by that email's body (long bodies are truncated with a notice).
2. Follow the vault conventions from Filing_Rules.md, provided in your system prompt if
   the file exists - it documents the concrete folder taxonomy, daily-note format, tag
   vocabulary, linking style, and create-vs-update rule.
3. Read /work/staging/personal/vault_index.txt - a complete, current list of every note in
   the vault, one per line as "YYYY-MM-DD<TAB>path" (the note's last-modified date). Use it
   as your primary create-vs-update tool: shortlist existing candidate notes by title and
   folder, then Read only those to confirm. A recently-modified note whose title matches an
   email's matter is likely the note to continue. Never copy the date column into a link.
   - Also read /work/staging/personal/recent_filing_log.md if it exists, and use
     search_relevant (thematic search) to find the note for an ongoing matter when its
     title doesn't match the email wording. Use grep_search only for exact identifiers.

TRIAGE (decide per email):
   - SKIP (do not file) noise: marketing and promotions, newsletters and digests,
     one-time passcodes / verification codes, automated system notifications and
     alerts, social-media notifications, and routine receipts/order confirmations
     with no lasting significance.
   - FILE anything with lasting personal significance: genuine personal
     correspondence, plans and bookings (travel, appointments, reservations),
     finance / health / legal events, and any commitment, decision, deadline, or
     action item - yours or someone else's - that you would want a record of.
   - When genuinely unsure, FILE it. But record every skip with its reason.

PROPOSAL (do not touch the vault - write only to /work/staging/personal/proposed.md):
4. For each email you would FILE: determine its DATE from the Received header (the
   Received timestamps are already in Singapore time (SGT, UTC+8) - use them as-is, no
   further offset), and decide exactly where it would be filed - which date's daily note (created if absent)
   and which topic/project note (existing UPDATE or new CREATE, with an idempotency
   marker <!-- personal-email:MESSAGE-ID --> (the Message-ID exactly as given, WITHOUT
   its surrounding < > angle brackets) and `[[links]]` to related notes). Use
   the YYYY-MM-DD (MMM) date format; compute weekdays from the date itself, anchored to
   the DATE CONTEXT above.
   - **DATA PRESERVATION RULE (LOSSLESS)**: For emails you would file, preserve substantive details - specific identifiers (booking references, amounts, dates, ticket/order numbers), names, exact quotes, concrete action items. Only omit true noise (signatures, tracking footers, boilerplate).
   - **NOTE GRANULARITY RULE**: Use highly specific note titles for distinct matters; never fold a distinct matter into a generic umbrella note. Create a new specific note unless the email is a direct continuation of that *exact same* matter.
   - **FILENAME RULE**: Note titles become filenames and wikilink targets. Never use the characters # ^ | [ ] : \ / in a note title (e.g. 'Flight refund case - REF784512', NOT 'Flight refund case - REF#784512'): '#', '^', '|' and brackets are Obsidian link syntax, so links to such notes silently break; ': \ /' are invalid in filenames on some platforms.
   - **EXACT-TITLE LINK RULE**: When linking to an EXISTING note, copy its title character-for-character from vault_index.txt (or the filename you read) - never retype it from the email's wording. A one-character difference (a space where the filename has an underscore, a reworded suffix) silently breaks the link.
   - **LINK INTEGRITY RULE**: If you write a `[[New Note Title]]` link anywhere, you MUST also output/create that new note file. Never leave a dangling link. When creating the new note, you MUST populate it with relevant operational details and context; do not just leave it blank.
5. For each email you would SKIP: one line with its subject and the skip reason.
6. Write your full proposal to /work/staging/personal/proposed.md - the only file you
   may write. Begin it with a PLAN TABLE: one row per email listing its subject and
   FILE (target daily note + any topic note, CREATE/UPDATE) or SKIP (reason). Then, for
   each email-to-file: name the target daily note and any topic note, CREATE or UPDATE,
   and the content that would be appended. Do not edit vault files.
7. Work efficiently: don't re-read files you've already read; keep commentary between
   tool calls to a minimum.
8. Use your available tools (read, glob, grep, write, search_relevant) to perform this task.
   Do not edit vault files.
9. When finished, call the finish tool with:
   {"status": "proposed" | "no_content", "work_date": "YYYY-MM-DD",
    "files_touched": ["/work/staging/personal/proposed.md"], "entries_filed": N,
    "skipped": M, "skipped_details": ["<Subject> — <reason>", ...]}
   - entries_filed = emails that would be filed; skipped = emails triaged out;
     skipped_details = one short line each. Always write proposed.md, even if the batch
     is entirely skipped (then status "no_content" and record the skips there).
