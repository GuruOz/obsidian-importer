You are filing tonight's M365 Copilot work-summary digest into this Obsidian vault.

{{DATE_CONTEXT}}

1. Read the staged digest at /work/staging/digest.md.
2. Follow the vault conventions from Filing_Rules.md, provided in your system prompt if
   the file exists - it documents the concrete folder taxonomy, daily-note format, tag
   vocabulary, linking style, and create-vs-update rule.
3. Read /work/staging/vault_index.txt - a complete, current list of every note in the
   vault, one per line as "YYYY-MM-DD<TAB>path" (the note's last-modified date). Use it
   as your primary create-vs-update tool: shortlist existing candidate notes by title and
   folder, then Read only those to confirm. A note whose date is recent AND whose title
   matches a workstream in tonight's digest is very likely the continuation target for it.
   Never copy the date column into a filename or wikilink.
   - Also read /work/staging/recent_filing_log.md if it exists: it lists which notes recent
     runs filed into, so a recurring workstream keeps landing in the same canonical note.
   - Use search_relevant (thematic/paraphrased search) to locate the canonical note for a
     workstream when its title doesn't match the digest wording - at most one call per
     workstream. Use grep_search only for exact identifiers (ticket numbers). Do NOT run
     broad vault-wide greps otherwise - the index already gives complete title coverage.
4. Determine the WORK DATE. If the DATE CONTEXT above specifies one, use it exactly.
   Otherwise infer it from the digest content itself (each section is headed
   "Digest received <timestamp>" and the body usually states its own date, in a
   "Work Log - <DD Mon YYYY>" style line). File everything under that date's daily
   note - creating it if absent - and use that date in the idempotency marker. Do
   not use today's date if the work date differs from it.
5. Before writing anything, check whether <!-- copilot-digest:<work-date> --> already
   exists in that daily note. If it does, make no changes and report that in your
   summary. Otherwise, add the marker as part of this run.
6. PLAN BEFORE WRITING: before you write to any vault file, write a filing plan to
   /work/staging/plan.md - one line per digest workstream/section: the target note path,
   CREATE or UPDATE, and a one-line reason. Only then execute the plan. If execution has
   to deviate, update plan.md so it always reflects what you actually did.
7. Split the digest into atomic entries (one discrete task/outcome/decision per entry)
   and append each under the correct daily-note heading.
   - **DATA PRESERVATION RULE (LOSSLESS)**: Do not summarize, compress, or omit operational details. Preserve all specific identifiers (ticket numbers like CS... or TrackingID...), names of individuals, exact quotes, and specific action items/options from the source digest. Only omit true noise (email signatures, contact details, confidentiality disclaimers). Do not alter acronyms or department names (e.g. preserve "ITD / IFNRA" exactly as written).
   - **CHRONOLOGICAL RULE**: When writing any date with a weekday, compute the weekday from the work date itself, anchored to the DATE CONTEXT above (you know today's date and weekday from it - count backward/forward). Never guess a weekday; a wrong weekday next to a correct date is worse than no weekday.
8. For entries that belong in a topic/project note, update the existing note (found via
   the index) or create a new one. Actively discover related existing notes via the index and embed `[[Note Title]]` links to inter-connect related concepts, issues, and tickets.
   - **WORKSTREAM CONTINUITY RULE**: A recurring workstream - the same initiative, rollout, escalation, or ticket appearing across multiple days (e.g. "Zscaler global classified-file upload block", "Purview agency-rule creation") - must APPEND to its single canonical topic note, not spawn a new note each day. Before deciding CREATE, you MUST check all three: (1) recent_filing_log.md, (2) a search_relevant call on the workstream's theme, (3) the index's recently-modified notes. Only CREATE when none of them surfaces a matching note.
   - **NOTE GRANULARITY RULE**: When creating new notes, use highly specific titles that describe the exact issue, task, or incident (e.g. 'RNS policy tips persist after licence removal', NOT generic titles like 'Purview policy tips limitation'). When deciding whether to UPDATE an existing note, you MUST NOT update notes with generic titles (like 'Purview policy tips limitation') just because they share a topic. If the digest introduces a specific scenario, test, or ticket, you MUST CREATE a new specific note rather than appending it to a generic umbrella note. Only update an existing note if it is a direct continuation of that *exact same specific issue or ticket*. (Tiebreaker with the rule above: same initiative/ticket -> UPDATE its canonical note; a genuinely new specific scenario under a broad theme -> CREATE.)
   - **NEW-NOTE TEMPLATE**: A new note under Work/*/Troubleshooting/ or Work/*/Testing/ must follow the vault's template shape: an H1 title line, a `**Date: <Weekday> <Do> <Month> <Year>**` line, a `##### keywords: <comma-separated people, systems, tickets>` line, a `---` rule, then the sections `### Who reported it:` / `### Problem statement:` / `### Troubleshooting steps taken:` / `### Solution:`, ending with a `### Related` section that wikilinks the work-date daily note and any sibling topic notes. Never add YAML frontmatter to any note - this vault uses none.
   - **FILENAME RULE**: Note titles become filenames and wikilink targets. Never use the characters # ^ | [ ] : \ / in a note title (e.g. 'Purview DLP escalation - TrackingID 2606080040000512', NOT '... - TrackingID#2606080040000512'): '#', '^', '|' and brackets are Obsidian link syntax, so links to such notes silently break; ': \ /' are invalid in filenames on some platforms.
   - **LINK INTEGRITY RULE**: If you write a `[[New Note Title]]` link anywhere, you MUST also output/create that new note file. Never leave a dangling link. When creating the new note, you MUST populate it with relevant operational details and context; do not just leave it blank.
9. Append only. Never rewrite, reorder, or delete existing content in any note.
10. Work efficiently: don't re-read files you've already read, batch independent edits,
   and keep commentary between tool calls to a minimum.
11. Use your available tools (read, glob, grep, edit, write, search_relevant) to perform this task.
12. When finished, call the finish tool with:
    {"status": "filed" | "skipped_duplicate" | "no_content", "work_date": "YYYY-MM-DD", "files_touched": [...], "entries_filed": N}

